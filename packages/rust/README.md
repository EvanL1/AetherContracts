# aether-contracts

Experimental Rust foundation binding for AetherContracts. It currently
provides canonical `u64` parsing with language-neutral failures and executes
the public CloudLink fixture manifest. It is not a complete production Thing
Model, authentication, or transport codec.
